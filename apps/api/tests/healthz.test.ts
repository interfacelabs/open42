import { describe, it, expect } from 'vitest';

/**
 * Example unit test. Real integration test for /healthz lives at
 * apps/api/src/routes/healthz.integration.test.ts (TODO — needs
 * testcontainers Postgres).
 *
 * Run with: npm run test
 */
describe('placeholder', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
